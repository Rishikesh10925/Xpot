<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Harshatrial"
        tools:targetApi="31">

        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".OwnerProfileActivity" />
        <activity android:name=".SuperProfileActivity" />
        <activity android:name=".BookingDetailsActivity" />
        <activity android:name=".UserBookingHistoryActivity" />
        <activity android:name=".OwnerBookingsActivity" />
        <activity android:name=".OwnerAnalyticsActivity" />
        <activity android:name=".SuperSecurityActivity" />
        <activity android:name=".SuperAnalyticsActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".UserDashboardActivity" />
        <activity android:name=".SuperAdminDashboardActivity" />
        <activity android:name=".AddParkingActivity" />
        <activity android:name=".MainActivity" />

    </application>

</manifest>